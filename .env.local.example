# Address lookup API key (optional)
# Get a free key at https://getaddress.io (20 lookups/day free tier)
# Without this key, the address lookup uses generated mock addresses for demo purposes
NEXT_PUBLIC_GETADDRESS_API_KEY=your_api_key_here
